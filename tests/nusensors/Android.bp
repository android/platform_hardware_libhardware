package {
    default_applicable_licenses: ["hardware_libhardware_license"],
}

cc_binary {
    name: "test-nusensors",

    srcs: ["nusensors.cpp"],

    cflags: ["-Wall", "-Werror"],

    shared_libs: [
        "libcutils",
        "libhardware",
    ],
}
